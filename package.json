{
  "name": "swapi-explorer",
  "version": "3.0.0",
  "description": "A clean, functional vanilla JavaScript explorer for SWAPI.",
  "type": "module",
  "scripts": {
    "start": "npx --yes http-server . -c-1 -o",
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^5.0.0",
    "jsdom": "^25.0.1",
    "vitest": "^5.0.0"
  }
}
